# skin selector issue

the dropdown does not select the configured skin on plugin reshow. Look is correct but dropdown entry is not.

# sidechain target label not aligned with what it does

Sees that drive selects gain ducking etc

# filters not having all the types supported

LP ok but notch and others seem to fail

# range to large for LFOs and LFOs setting hard to control

The range is too hard to deal with and lower values should allow detail il low Hz values like 0.1, 0.2...
