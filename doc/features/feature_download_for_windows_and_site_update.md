# Feature download for windows

To Download for windows, one needs a release package from a cmake build

That build has to be packaged.

Packaging happens using InnoSetup (free)

Need an automation pipeline for this to not be a pain in the ass.

Website should be automatically updated when a new version is released.
This means that the whole site should be re-uploaded to firebase, and a changelog uploaded to the repo.