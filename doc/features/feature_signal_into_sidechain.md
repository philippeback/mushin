Signal into sidechain

The test signal panel audio can also become the sidechain input.
So we have external/internal/test signal options.

This allows to also test the side chain without having to get an external signal fromn the DAW